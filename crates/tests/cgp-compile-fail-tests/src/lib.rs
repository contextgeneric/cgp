//! Compile-fail tests for the CGP macros, driven by `trybuild`.
//!
//! This crate carries no library code — the suite lives in the `trybuild`
//! fixtures under `tests/` and their driver `tests/compile_fail_tests.rs`. See
//! the crate [README](../README.md) for what belongs in each category and how to
//! regenerate the `.stderr` snapshots.
